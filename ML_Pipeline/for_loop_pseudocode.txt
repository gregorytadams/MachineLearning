

list_of_models = [logit, svm(C=1), svm(C=10), svm(C=100), svm(c=1000), ]

The big for loop (data, response, predictors, list_of_models, how_many_splits):
	for i in range(how_many_splits)
		for subsets of features:
			x, y = format_for_models
			for i in list_of_models:
				fit model
				predict
				evaluate model on accuracy, precision, recall and auc
				output
	return best model! 